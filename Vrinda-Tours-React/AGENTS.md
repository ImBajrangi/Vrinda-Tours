- **Geolocation Throttling Rule**: Always throttle real-time geolocation streaming to update database records only upon moving 15+ meters or every 10+ seconds.
- **Native Browser Dialog Ban**: Replace native browser `window.confirm` and `alert` dialogs with custom styled modal overlays for seamless UX.

